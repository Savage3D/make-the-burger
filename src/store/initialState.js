import pickerStatuses from '../util/pickerStatuses';

const initialState = {
	items: ['White bun top', 'Sliced tomato', 'Ground-beef patty', 'White bun bottom'],
	selectedIndex: -1,
	pickerStatus: pickerStatuses.CLOSED,
};

export default initialState;
